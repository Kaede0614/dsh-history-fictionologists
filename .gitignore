# ============================================================================
# 仓库只分发「插件本体 + 测试 + 文档」。
# 下面被忽略的目录仍保留在本机工作区（绝不删除），只是不进入版本库。
# 原因见 README「仓库里有什么 / 没有什么」一节。
# ============================================================================

# ---- Node ----
node_modules/
*.tgz
npm-debug.log*
pnpm-debug.log*
.pnpm-store/

# ---- 工作区本地数据：游戏原始文本（米哈游版权内容，20+ MB）----
# 功能 2（构史文集）与功能 3（星际构史播报）靠它避让既有故事冲突。
# 体积与版权都不适合随仓库分发，请自备（README 有说明）。
hsr-missions/

# ---- 工作区本地数据：Wiki 抓取缓存 ----
# 由 gs_update 增量抓取生成，可随时重建；只有手工维护的裁定层入库。
hsr-worldview-cache/*
!hsr-worldview-cache/user-canon.json

# ---- 工作区本地数据：本机生成的成品（个人创作，不入库）----
hsr-stories/
hsr-broadcasts/

# ---- 开发期结构侦察产物（4.9 MB 原始渲染 HTML）----
# 是 docs/DESIGN.md 里每条选择器的原始证据，但体积大且属抓取快照，
# 需要时按 docs/DESIGN.md 的记录重抓。
_probe/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.bak
*.orig
*.log
Thumbs.db
Desktop.ini
.DS_Store
